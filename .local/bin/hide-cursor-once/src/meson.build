sources = ['hide-cursor-once.cpp']

hide_cursor_once = shared_module('hide-cursor-once', sources,
    dependencies: [wayfire],
    install: true, install_dir: join_paths(get_option('libdir'), 'wayfire'))

subdir('client')
