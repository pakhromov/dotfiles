"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
exports.__esModule = true;
exports.MpvJsonIpc = void 0;
var events_1 = require("events");
var MpvJsonIpc = /** @class */ (function (_super) {
    __extends(MpvJsonIpc, _super);
    function MpvJsonIpc(socket) {
        var _this = _super.call(this) || this;
        _this._callbacks = new Map();
        _this._requestId = 0;
        _this._onData = function (data) {
            var lines = data.toString().split('\n');
            for (var _i = 0, lines_1 = lines; _i < lines_1.length; _i++) {
                var line = lines_1[_i];
                var response = void 0;
                try {
                    response = JSON.parse(line);
                }
                catch (e) {
                    return;
                }
                if ('event' in response) {
                    _this.emit(response.event, response);
                    return;
                }
                if (typeof response.request_id === 'number') {
                    var cb = _this._callbacks.get(response.request_id);
                    if (cb) {
                        _this._callbacks["delete"](response.request_id);
                        try {
                            cb(response);
                        }
                        catch (e) {
                            // Do nothing
                        }
                    }
                }
            }
        };
        _this.command = function () {
            var args = [];
            for (var _i = 0; _i < arguments.length; _i++) {
                args[_i] = arguments[_i];
            }
            _this._requestId = (_this._requestId + 1) % (Math.pow(2, 32) - 1);
            var cmd = {
                command: args,
                'request_id': _this._requestId
            };
            return new Promise(function (resolve, reject) {
                var cb = function (response) {
                    resolve(response);
                };
                _this._callbacks.set(_this._requestId, cb);
                try {
                    _this.socket.write(JSON.stringify(cmd) + '\n');
                }
                catch (e) {
                    reject(e);
                }
            });
        };
        /**
         * Removes any event-listener installed on the socket.
         */
        _this.destroy = function () {
            _this.socket.off('data', _this._onData);
        };
        _this.socket = socket;
        socket.on('data', _this._onData);
        return _this;
    }
    return MpvJsonIpc;
}(events_1.EventEmitter));
exports.MpvJsonIpc = MpvJsonIpc;
//# sourceMappingURL=index.js.map